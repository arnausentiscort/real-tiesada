# Real Tiesada FC — project instructions

All project context lives in a single file: **[`CLAUDE.md`](CLAUDE.md)**.

Read it before changing anything. It covers the project rules, the stack, the file
architecture, the data structures, the squad, the fixture list and the deploy commands.
It is written in Catalan, which is also the language of the app's UI.

> This file used to be an English copy of `CLAUDE.md` and drifted out of date: it still
> described the Split 2 (25/26) futsal season, the nav with MVP / Mapa de Gols / Galeria,
> `'pissarra'` routing straight to `TacticalBoard`, and R=22 board tokens. None of that
> holds any more. It is now just a pointer — **add new context to `CLAUDE.md`, not here.**
